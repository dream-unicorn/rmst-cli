{
  "name": "try",
  "version": "1.0.0",
  "main": "index.js",
  "license": "",
  "scripts": {
    "start": "rmst-cli start",
    "rmst-cli": "npx --ignore-existing rmst-cli"
  },
  "devDependencies": {
    "rmst-cli": "@latest"
  }
}
